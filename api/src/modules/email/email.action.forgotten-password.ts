import { Repository } from 'typeorm'

import { ResetPassword } from '../reset-password/reset-password.entity'

export const getForgottenPasswordInfo = async (payload) => {
	/*
    return {
      firstName: request.user.profile ? request.user.profile.firstName : request.user.email,
      link: `${config.app.clientUri}/reset-password?code=${request.code}`,
      assistanceLink: `${config.app.clientUri}/contact`,
      ...getDefaults(),
    }
    */
}
